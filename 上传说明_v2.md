# 上传说明 v2（`4.24-cli` 分支）

## 📦 zip 里有什么

整合好的 `Redream-project-gen/`，**86 个文件**。

### 关键变化（对比 4.24 分支）

| 项 | 4.24（旧） | 4.24-cli（新）✨ |
|----|-----------|------------------|
| phase1-figma-json | 7 份 + README（4.22 版，1372 行）| **8 份 + README（v20 版，1844 行）**|
| 按钮命名规则 | v19（底板_xxx FRAME）| **v20（按钮_xxx FRAME）**|
| references/figma-to-red-*.md | `plistlib.md`（手写 XML）| **`cli-driven.md`（CLI 驱动）**|
| 新增 lessons | 9 份 plistlib | **10 份 CLI 驱动 + 5 份保留通用** |
| 删除 lessons | — | 4 份过时 plistlib（no_hand_built_xml 等）|

### 新增的 10 份 CLI lessons

1. `lesson_cli_param_order.md` — options 必须在 action 前
2. `lesson_cli_help_vs_actual.md` — --json 帮助里有但不支持
3. `lesson_new_project_resource_paths.md` — 必须手动加 Resources 路径
4. `lesson_cli_subcommand_hierarchy.md` — list-node-types 在 modify 下
5. `lesson_export_vs_generate_json.md` — 生成 vs 导出 JSON 不能混用
6. `lesson_airplay_port_5000.md` — macOS AirPlay 占端口
7. `lesson_zombie_python_process.md` — Flask 退出不干净
8. `lesson_file_vs_http_protocol.md` — 不能双击 HTML
9. `lesson_prefered_size_unit.md` — preferedSize 必须和 contentSize 单位一致
10. `lesson_figma_al_child_xy.md` — Figma AL 子节点有 x/y（不用模拟）

### 保留的 5 份通用 lessons

- `lesson_unit2_auto_convert.md`
- `lesson_unit3_content_size_only.md`
- `lesson_button_content_as_child.md`
- `lesson_floating_layer_mask.md`
- `lesson_coord_constraint_flow.md`

---

## 🚀 上传步骤

### 1. 解压 zip
解压到临时目录，得到 `Redream-project-gen/` 文件夹。

### 2. 替换本地 clone 的内容
和上次一样：
- 打开 `/Users/red/Desktop/Redream-Project-Gen/`（外层，大写 R）
- 删掉里面的 `Redream-project-gen/`（内层，小写 r）
- 把新解压的 `Redream-project-gen/` 拖进去

### 3. 在 Fork 里切到 main，然后从 main 新建 4.24-cli 分支

⚠️ **关键：从 main 开分支，不是从 4.24！**

为什么？你的 4.24 分支已经推出去给同事了，v2 是**另一个独立方案**。两个分支并列，都基于 main（空的 skill 状态）。

**步骤：**

1. Fork 左侧 **Branches** → 双击 `main` 切过去
2. 如果 main 显示有 commit（不是空的），先 **Pull** 同步远端最新状态
3. 右键 `main` → **New Branch from "main"**
4. 名字填：`4.24-cli`
5. 勾选 **Checkout after create**
6. 点 **Create Branch**

**重要**：切到 4.24-cli 分支后，你本地的文件可能还是 4.24 分支的内容（因为你替换文件时在 4.24 分支）。这种情况：
- 先看 Fork 的 **Local Changes** 有没有东西
- 如果有 → 说明当前文件和 4.24-cli 分支（刚从 main 建的，是空的）不同，正好就是我们要提交的改动
- 如果想确认：点 **Local Changes** 应该能看到 86 个新文件

### 4. Commit

1. 左侧点 **Local Changes**
2. 全选 + **Stage**
3. Commit subject：
```
2026-04-24 RED Tool CLI 化迁移 v2
```
4. Description：
```
phase1 升级 v20（按钮_xxx FRAME 替代 底板_xxx FRAME）
RED Tool 架构迁移到 CLI 驱动（build-scene --config）
新增 10 份 CLI lessons + 保留 5 份通用 lessons
淘汰 4 份 plistlib 专用 lessons
references/figma-to-red-plistlib.md → figma-to-red-cli-driven.md
```
5. 点 **Commit**

### 5. Push

1. 顶部 **Push**
2. Branch：`4.24-cli` ✅
3. To：点下拉 → **Custom Remote Branch Name** → `4.24-cli` → Add
4. 确认 To 显示 `origin/4.24-cli`（不是 Neo / main / 4.24）
5. 点 **Push**

---

## 💬 推完告诉同事

```
俊玮，RED Tool 这两天又更新了一版，我把新 skill 推到了 4.24-cli 分支：

主要改动：
- phase1 升级到 v20 按钮规则（按钮_xxx FRAME），和引擎 is_btn_layer 对齐
- RED Tool 从 plistlib 手写迁移到 CLI 驱动（用 build-scene --config 规范化）
- 新增字段命名铁律（下划线 vs 驼峰对照）、触摸点不可见兜底、组件预检 等

和 4.24 分支是独立的两个方案，你对比着看哪个更合适，如果 4.24-cli 更好就废掉 4.24。
```

---

## ⚠️ 如果你想让 4.24-cli 基于 4.24 而不是 main

（也就是让它包含 4.24 的 commit + v2 的新增 commit）

改第 3 步：
1. 切到 **4.24 分支**（不是 main）
2. 右键 `4.24` → **New Branch from "4.24"**
3. 名字填 `4.24-cli`
4. 继续后续步骤

这样 4.24-cli 会先有 4.24 的那条 commit，然后加上 v2 的新 commit（两条）。

**取决于你想展示给同事什么**：
- 从 main 开：纯净的 v2（一条 commit，他单独对比）
- 从 4.24 开：v1 + v2 的演进链（两条 commit，他能看到你的迭代过程）

**我建议从 main 开**（方案 A），让两个分支互相独立、好对比。
