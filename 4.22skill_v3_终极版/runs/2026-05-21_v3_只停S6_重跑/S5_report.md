# S5 · 提取 flow — 报告

## 表 F: 屏幕清单 (修订最终版)

| 序号 | 屏幕名 | 类型 | 帧范围 | 来源 |
|---|---|---|---|---|
| 1 | 界面_团队宝藏 | 界面_ | 0..8.8s (frame_00..frame_25 全部) | S1 初识, 视频全程 |

**屏幕类型判断**: 全屏替换型 (从视频帧看, 占满屏幕, 顶部装饰带做主视觉) → **`界面_`** 前缀, 转场动画 default = `dissolve 300ms`

**帧差分检测**: 视频 26 帧均无 >40% 差分 (列表滚动只导致列表区局部变化, 总差分远小于 40%) → 单一屏幕, 无转场。

---

## 表 G: flow 连线表

**无连线**: 视频仅 1 个屏幕, 全程未发生屏幕跳转。视频内交互仅包含 3 次列表 VERTICAL 滚动手势 (S4 已记录, 不是 flow 跳转)。

---

## 候选 flow (用户后续如需扩展, 留给 S7-S9 决定)

视频未触发但 UI 存在的潜在跳转入口 (留给 S7/S9 是否写入 flow 数组):

| # | 触发元素 (描述性) | 推测目标屏 | 推测动画 | 备注 |
|---|---|---|---|---|
| C1 | 按钮_关闭候选 (右上 红 X) | 上级屏幕 (未知, 视频无信息) | `CLOSE` | 视频未触发 |
| C2 | 按钮_信息候选 (左上 蓝 i) | `浮层_团队宝藏说明` (推测) | `dissolve 300ms OVERLAY` | 视频未触发 |
| C3 | 子 ccb 状态_可领取 (variant 3, 按钮+claim) | 可能触发 `浮层_领奖` 或本地状态变更 | `dissolve 300ms OVERLAY` 或无切屏 | 视频未触发, 用户预声明该 variant 存在 |
| C4 | 宝箱_1/2/3 INSTANCE (进度条节点) | 推测点击 → 弹出预览浮层 | `dissolve 300ms OVERLAY` | 视频未触发 |

⚠️ 以上 4 条均**视频未触发**, 来源未确认, 不写入正式 flow。 S7 阶段如果需要把它们标记为 REDNodeButton (`按钮_` 前缀), 可以保留, 但 `flow` 数组留空。

---

## 验证

1. ✅ 屏幕清单完整: 1 屏, 无遗漏
2. ✅ flow 连线覆盖: 起始屏无需指入连线
3. ✅ 转场追溯: N/A (无转场)
4. ✅ 候选连线占比: 100% 候选 (视频未触发任何点击), 提醒用户**下次录屏开启 Show Taps + 演示完整点击流程**, 才能识别 flow 跳转

---

## 提示用户

本视频仅展示界面初始状态 + 3 次列表滚动, **未触发任何点击交互**。若需识别按钮 flow 跳转:
- iOS: Settings → Developer Options → Show taps (开启触摸点显示)
- 录制完整交互流程: 点击按钮 → 等待跳转完成 → 点击下一个按钮

当前 skill 流程仍可继续 (S6 pattern 命中 + S7-S11 生成 scene.json), flow 字段将留空 (`flow: []`)。
